<h1>Editing Production</h1>

<%= form_for(@production) do |f| %>
 <% if @production.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@production.errors.count, "error") %> prohibited this production from being saved:</h2>
      <ul>
      <% @production.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= f.text_field :productionType, class: 'form-control' %>

  <div class="actions">   
    <%= f.submit "タイプ名　編集", class: 'btn btn-primary' %>
  </div> 
<% end %>

<%= link_to 'Show', @production %> |
<%= link_to 'Back', productions_path %>
